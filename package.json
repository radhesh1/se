{
  "name": "your-full-stack-app",
  "version": "1.0.0",
  "description": "Your Full Stack App Description",
  "main": "server.js",
  "scripts": {
    "start": "node ./backend/server.js",
    "client": "npm start",
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "mongoose": "^6.0.10",
    "iframe-resizer-react": "^3.6.3"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
